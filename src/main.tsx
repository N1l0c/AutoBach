import React from 'react';
import ReactDOM from 'react-dom/client';
import AutoBach from './AutoBach';
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AutoBach />
  </React.StrictMode>
);